var data = require('data');
var React = require('react');
var ReactDOM = require('react-dom');
import $ from 'jquery';
// import subtract from 'subtract';
import print from 'index-cmd2-js';

ReactDOM.render(
  <h1>{data}</h1>,
  document.body
);
